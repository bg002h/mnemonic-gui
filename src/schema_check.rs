//! Runtime soft-check: shell out to each detected CLI's `--version` and
//! compare against `Schema.pinned_version`. Render banner on mismatch.
//! Phase 9 wires it; Phase 6 may stub.

pub fn placeholder() {}
