//! Secret-handling modals + zeroize sweep (SPEC §9). Phase 7 fills it and
//! re-exports `SECRET_NODE_TYPES` + `SECRET_SLOT_SUBKEYS` from the build.rs-
//! generated `secrets_generated.rs`.

pub fn placeholder() {}
