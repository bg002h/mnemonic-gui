//! Schema types (`Schema`, `SubcommandSchema`, `FlagSchema`, `FlagKind`) +
//! per-CLI schema constants. Phase 1 fills the types; Phases 1/6 fill the
//! per-CLI constants.

pub mod md;
pub mod mk;
pub mod mnemonic;
pub mod ms;
