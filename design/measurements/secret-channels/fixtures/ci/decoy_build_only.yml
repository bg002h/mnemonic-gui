# R3 decoy: the targets are built, never run
on: [push, pull_request]
jobs:
  mac:
    runs-on: macos-latest
    env:
      MNEMONIC_BIN: mnemonic
    steps:
      - run: cargo build --test secret_channels_t2 --test secret_channels_t3prime --test secret_channels_t7
